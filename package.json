{
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:translate": "go run translate.go",
    "docs:build:all": "TRANSLATION_TIMEOUT=30 TRANSLATION_MAX_RETRIES=0 npm run docs:translate && npm run docs:build",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "openai": "^4.90.0",
    "vitepress": "^2.0.0-alpha.19"
  }
}
